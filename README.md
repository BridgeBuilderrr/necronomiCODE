# cara menjalankan 
npm install
npm start
# server berjalan di http://localhost:3000
jangan lupa ditambahkan /bahasa di akhir server local host

Uji dengan `curl` atau Thunder Client, contoh:

```bash
curl http://localhost:3000/bahasa
```